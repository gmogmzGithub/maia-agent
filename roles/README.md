# Hermes Roles

This directory contains public-safe Hermes role/profile material.

Role text may describe how Hermes should reason and which tools it may request.
It must not contain credentials, private customer data, raw sessions, or Product
business rules that belong in `src/realestate/domain`.
