# Public Site

This package is Maia's GUI/web frontend layer.

It contains the server-rendered public site, templates, static assets, and the
Product gateway used by the standalone Site process. It owns presentation and
browser interaction, but no catalog truth, customer identity, database writes,
or provider credentials.
